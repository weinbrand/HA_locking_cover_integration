# HA_locking_cover_integration
Locking Cover — Home Assistant Custom Integration  Wraps an existing `cover` entity (e.g. a **Shelly Plus 2PM** in cover mode) and adds a mechanical bolt-locking layer on top of it, without modifying the wrapped cover's own integration.
